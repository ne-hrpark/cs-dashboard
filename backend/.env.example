# DB 없이 실행 (프론트 개발용)
MOCK=true

# 실제 DB 사용 시 아래 설정 후 MOCK=false 또는 MOCK 제거
# MOCK=false
# DB_SERVER=localhost
# DB_PORT=1433
# DB_USER=
# DB_PASSWORD=
# DB_NAME=
# DB_ENCRYPT=false

# 서버
PORT=3001
# NODE_ENV=development

# 배포 시 CORS 허용 도메인 (쉼표 구분)
# CORS_ORIGIN=https://your-domain.com

# (선택) API 인증 — auth.guard.ts에서 사용
# API_KEY=your-secret-api-key
