-- ============================================================
-- GET /api/users - 회원 목록 총 개수 (페이징용)
-- ============================================================
-- userList.sql과 동일한 WHERE 조건을 적용해 총 레코드 수를 반환합니다.
-- 필터 적용 시 이 쿼리에도 동일한 WHERE를 넣어주세요.
-- ============================================================

SELECT COUNT(*) AS total
FROM Members;
-- WHERE (userList.sql과 동일한 조건)
