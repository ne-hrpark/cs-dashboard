# API 베이스 URL (하드코딩 금지)
# - Local: /api (Vite 프록시 → backend)
# - Vercel Production: 백엔드 실제 URL (예: https://api.example.com)
VITE_API_URL=/api

# local | server
# - local: Vite dev 서버에서 /api 프록시 사용
# - server: VITE_API_URL 그대로 사용 (Vercel 등)
VITE_APP_ENV=local
